# Domain Package

This package contains domain business logic:
- Domain Models representing core entities (Task, Habit, UserProfile, LevelProgress).
- Repository Interfaces defining data contracts.
- Use Cases & Business Rules for XP calculation, leveling up, streak tracking, and daily resets.
