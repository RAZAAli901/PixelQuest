# Data Package

This package contains data layer implementations:
- Room Database & DAOs for local persistence (tasks, habits, user stats).
- Repository implementations connecting data sources to domain interfaces.
- Preferences DataStore for user settings and theme options.
